import { formatMonth } from 'my-calendar/helpers/format-month';
import { module, test } from 'qunit';

module('Unit | Helper | format month');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = formatMonth([42]);
  assert.ok(result);
});
