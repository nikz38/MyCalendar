import { formatEventTime } from 'my-calendar/helpers/format-event-time';
import { module, test } from 'qunit';

module('Unit | Helper | format event time');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = formatEventTime([42]);
  assert.ok(result);
});
