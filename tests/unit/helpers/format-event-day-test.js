import { formatEventDay } from 'my-calendar/helpers/format-event-day';
import { module, test } from 'qunit';

module('Unit | Helper | format event day');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = formatEventDay([42]);
  assert.ok(result);
});
